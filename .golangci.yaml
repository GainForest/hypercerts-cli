linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - unconvert
    - gofmt
    - goimports

linters-settings:
  goimports:
    local-prefixes: github.com/hypercerts-org/hyper

issues:
  exclude-use-default: false
